[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["cd backend && sh ./mvnw clean package -DskipTests=true"]

[start]
cmd = "cd backend && java -Dserver.port=$PORT -jar target/full-project-0.0.1-SNAPSHOT.jar"
